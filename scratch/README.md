# Local scratch

Practice files, one-off experiments, and personal notes live here — not in `phases/*/code/`.

This directory is gitignored except for this README. Copy anything you want to keep; nothing else in `scratch/` is committed.
